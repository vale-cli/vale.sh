# FAQ

Answers to questions Vale users ask most often.

{% hint style="info" %}
If none of these covers your case, ask in [Discord](https://discord.gg/tPeMs4A).
{% endhint %}

## Scopes and markup

### Is there a way to lint the url in a link?

No, there is not a more specific scope than `raw` for this case.

I think this is beyond Vale's purpose as a generic (many markup inputs) _prose_ linter and ventures into the space of an AsciiDoc-specific linter.

### A question around `BlockIgnores` / getting `vale` to play nicely with Docusaurus

Somehow, our Docusaurus doesn't play nicely with Vale ignore comments. So be it, I've found another solution:

**`tl;dr`: The solution**
Using four backticks for code blocks instead of three backticks makes the `BlockIgnores` shown above work as expected (at least as I expected it to work 😉 ).

**The explanation**
I wanted to find the cause for this issue, so I installed some go-tooling and got the Intellij-Go-IDE (GoLand) to debug the project for me (I'm not good at GoLang, but I can use Intellij-like IDEs 😜 ).

What I saw was that `vale` "ignores" `BlockIgnores` statements by wrapping them in code blocks. This, however, caused interference when such blocks were found in an already existing code block:

This:…

### How to accept a company name, company email address and social media handle at the same time

This is a little tricky due to how tokens like `@vaadin` are split, but you could change your vocab entry to `(?-i)Vaadin`. This will have the effect of strictly allowing the use of "Vaadin" to pass `Vale.Spelling` while also avoiding the case suggestions for instances of `@vaadin`.

### Setting up asciidoctor as parser for Vale

All you need to do is ensure that `asciidoctor` is available on your `$PATH` (and it should be if you used any standard installation method).

### Could you explain how do you parse Asciidoc and obtain linenumbers?

Vale doesn't actually do any parsing of AsciiDoc; it operates entirely on HTML generated by Asciidoctor.

### Is it possible to disable spell checking alt attribute values?

You can define a [`TokenIgnores`](https://docs.errata.ai/vale/config#tokenignores) pattern:

```ini
[*.adoc]
TokenIgnores = (image::.+\[\])
```

Of course, the best "workaround" here is probably to provide accessibility-friendly alt text.

### How to properly lint javadoc with vale ?

Javadoc isn't a supported format, so there's not going to be a perfect way to do this.

### Ignore in yaml file

Since YAML isn't one of Vale's supported file formats, there aren't many options for ignoring content. You'll need to either create a modified version of the rule or add `TRUE` as an exception.

### Wondering if it is possible to write a style/rule that checks for the *existence* of ALT text

If you use `scope: raw` you can write rules that target the underlying raw markup. So, something like

```yaml
extends: existence
message: "'%s' does not have an alt text."
level: warning
scope: raw
raw:
  - '!\[\]\(.+\)'
```

should work.

### Existence rule alert when vocab word in expression

No, adding a token to an `accept.txt` file is basically making it a global exception. If you need it to only be an exception for a single rule, you can add it to a specific file instead of the vocab.

### Home Directory Not Working with Config File

This actually needs to be `%UserProfile%`, as [described here](https://pkg.go.dev/os#UserHomeDir). I've updated the docs to reflect this.

### Possible to ignore other reStructuredText directives/roles?

You can do this with a [`BlockIgnores`](https://vale.sh/docs/topics/config/#blockignores) pattern:

```ini
[*]
BasedOnStyles = Vale

BlockIgnores = (?s) *(\.\. math::)
```

### How to apply the same rules to code and .rst files using just one file?

No, but the next release of Vale will allow `raw`-scoped rules to run on source code too.

### Using markup in Vale rule seems erratic

If you need to match against markup syntax (e.g., words inside backticks here), you'll want to use `scope: raw` to bypass any markup processing.

### Any documentation on how tokenization interacts with rules?

Neither of these questions have a general answer.

It depends on the content and `extends` value of your rule. The process for `existence` [is described here](https://vale.sh/docs/topics/styles/#existence). You can also use [Vale Studio](https://medium.com/valelint/vale-studio-2-0-cb33e1abca95) to view the final regex.

This depends on the value of `scope`. By default, it is all text with markup syntax removed.

### Could a Specific Reference Style Be Enforced in an Asciidoc File?

Yes, linting markup syntax is possible when using `scope: raw`. There are some AsciiDoc-specific examples here: https://github.com/rohennes/vale-asciidoc

### How to Ignore All Code Formatted Text in Asciidoc?

Vale will ignore [listing blocks and inline literals by default](https://vale.sh/docs/topics/scoping/#asciidoc).

If you specify `scope: raw`, though, you get the "raw" unprocessed file contents, so no scope-related features will work. In this case, your rule should work as intended if you remove `scope: raw`:

```yaml
extends: existence
message: "Use uppercase letters for hexadecimal numbers"
level: error
raw: '\b0x[0-9a-f]*[a-f][0-9a-f]*\b'
```

### TypeScript support

Can you try removing the format assignment

```
[formats]
ts = js
```

entirely?

### How to validate words that have just a letter formatted in bold?

You can turn the rule off around the particular paragraph [using comments](https://vale.sh/docs/topics/config/#markdown-amp-html):

```markdown

<!-- vale Custom.spelling = NO -->

The three pillars of Developer Relations are **C**ommunity, **C**ontent, and **C**ode, also known as the 3Cs.

<!-- vale Custom.spelling = YES -->
```

### A custom WordTemplate including a space

What I'd do in this case is assign an [embedded markup syntax](https://vale.sh/docs/topics/scoping/#code-1) to the comments:

```ini
[formats]
**Treat .cc comments as Markdown**
cc = md

[*.cc]
**You then have access to all Markup-related features,**
**such as TokenIgnores and BlockIgnores.**
TokenIgnores = (\\c \w+)
```

I'm not familiar enough with qdoc markup to know if there's any significant syntax incompatibilities with Markdown, but it doesn't seem like it.

It's also possible that qdoc support could be added natively to Vale. Is there a command-line toolchain for converting qdoc to HTML?

### Help with conditional rule: Check whether variable is unused

While this feels like something that *shouldn't* be done using Vale (it's more appropriate for an MDX syntax linter), it is possible:

```yaml
extends: conditional
message: "'%s' has been imported but not used."
level: error
scope: raw
first: "(?<=import )(.*)(?= from)"
second: '(?<=<)(\w+)'
```

### Setting the markup language for a file not on stdin.

Yes, but this will only work from the command-line and with single-file or all `rst` input.

Another approach is to use the [format association](https://vale.sh/docs/topics/config/#format-associations) section:

```ini
[formats]
txt = rst
```

This will work everywhere (e.g., editor extensions) and with multi-format input.

### Create Vale rule from working regex: How to conditionally match "Internet"

```yaml
extends: existence
message: "'%s' should only be capitalized when starting a sentence."
level: error
nonword: true
scope:
  - heading
  - list
  - sentence
tokens:
  - (?<=\s)Internet(?! Service Provider| Protocol)
```

See also this [Vale Studio session](https://studio.vale.sh/s/05647f41f327c87cfaca1b671d50ce79).

### Test Vale rules for headings with Vale Studio?

Worked it out. It seems you need at least a valid HTML doc, although nothing beyond that.

### How to ignore comments in a key=value file

`IgnoredScopes` and `TokenIgnores` only work for [supported file formats](https://vale.sh/docs/topics/scoping/#formats).

### "Occurrence" not working as expected?

As it turns out, yes, this was a misunderstanding on my part. I was wanting this to search the entire doc, but the `scope` was where I was getting confused. In the above example, it will search each heading to ensure that the token isn't present multiple times _in that heading_. To get what I was looking for, I needed to increase the scope - the following seems to do what I was originally going for:
```
extends: occurrence
message: "Problem description heading does not occur exactly once"
ignorecase: true
scope: raw
level: error
min: 1
max: 1
token: "<h[1-9]>problem description"
```

### Using markup-based configuration for style rules with the `raw` scope

Comment processing is done after a document is converted to HTML. Rules using the `raw` scope aren't run on the converted document, which means that no markup-related features (comments, ignore patterns, etc.) will work.

However, you should only need to use the `raw` scope when targeting markup *syntax* itself. So the cases should be pretty rare and specific.

### Check if a word is italicized

The `raw` scope allows you to write rules that are directly applied to the unprocessed file contents, meaning you can target markup syntax itself. So, for example, you'd do something like this:

```yaml
extends: existence
message: "'%s' shouldn't be italicized."
scope: raw
nonword: true
tokens:
  - '\*(?:word1|word2)\*'
 ```

### Vale ignores vocabulary accept.txt, Why?

Your regex doesn't match anything. It looks like you want, for example, `[dD]eserializers?` instead of `[dD]eserializer(s)`.

### TokenIgnores for AsciiDoc Cross References

In the first case, the HTML produced is:

```html
<p>See <a href="#ecc_memsys_error_path">Subchapter</a> for an exemplification of the ECC logic within the pipelines.</p>
```
Where "Subchapter" is the text linted.

In the second case, it's:

```html
<p>See <a href="#ecc_memsys_error_path">[ecc_memsys_error_path]</a> for an exemplification of the ECC logic within the pipelines.</p>
```
Where "[ecc_memsys_error_path]" is the text linted.

---

My suggestion would be to explicitly provide a label that is suitable for linting:

```asciidoc
= Chapter

See <<ecc_memsys_error_path,My Label>> for an exemplification of the ECC logic within the pipelines.
```

### Help linting YAML files please?

You can create a custom file [view](https://vale.sh/docs/views) that extracts those sections:

```yaml
engine: dasel
scopes:
  - name: chapter.title
    expr: chapters.all().title

  - name: chapter.goal
    expr: chapters.all().goal
    # Long form content (`|-`) might contain markup?
    type: md

  - name: section.objective
    # objectives can be empty
    expr: chapters.all().topics.all().sections.all().objectives?.all()
```

You can then write rules that target them individually:

```yaml
extends: existence
message: "'%s' should be capitalized"
scope: chapter.title
raw:
  - "^[a-z].+"
```

Or just let your default (`text`-scoped) rules run on these setions.

## Writing rules

### Lint specific portion of the doc

It looks like _scoping_ could be the answer to my query. Can somebody confirm?
Also, is it fine to specify multiple tags in the scope. For example, in DITA a procedure is within the `steps `tag and in HTML it's in the `ol `tag.

### Detecting multiline input

If you could provide an example of what you're trying to do (or what you've tried to do), it would be easier for me to help.

That said, as of [v2.9.0](https://github.com/errata-ai/vale/releases/tag/v2.9.0), you *can* use lookarounds in `existence`-based rules.

### Org mode configuration of source blocks

Org is not currently a supported format, so markup-related features won't work. See #330 for related information.

### Troubleshooting Sequence to find preposition at beginning of sentence

There are two problems: 

- `pos` is not a [supported key](https://vale.sh/docs/topics/styles/#sequence); you likely meant `tag`.
- `^` matches the start of a line, not a sentence.

With the latest release, you can now use something like this:

```yaml
extends: sequence
message: "Sentence should not start with a preposition."
tokens:
  - pattern: '[A-Z][a-z]+'
    tag: PRP
```

### Extend example conditional rule to accept plurals?

```yaml
extends: conditional
message: "'%s' has no definition"
level: error
ignorecase: false
first: '\b([A-Z]{3,5})(?=s\b|\b)'
second: '(?:\b[A-Z][a-z]+ )+\(([A-Z]{3,5})s?\)'
```

### Exceptions not working in substitution extension?

This seems to work:
~~~
swap:
  (?<!when |initial |a |default |root |your )login: log in
  you login: you log in
  log into: log in to
  logout (?:off|from): log out of
  logging into: logging in to
  logging out from: logging out of
~~~

I had to fiddle it about a bit, and without being able to capture groups I had to write extra tests (hint hint :smile: ) but it seems to be doing all the right things.

Thanks again.

### Ignore rule from the CLI?

If you want to do this permanently, use `style.rule = NO` (e.g., `Google.DateFormat = NO`) in your `.vale.ini`. 

If you're looking to do this as a one-off from the command line, use a [filter](https://vale.sh/manual/filter/): `vale --filter='.Name != "demo.Cap"'`.

### How to use the same %s token twice in a message

Got it!

`message: "Avoid the use of '%s' on weekdays. Only use '%[1]s' on weekends."`

### Help with `sequence` extension?

I think you want to `negate` the final token:

```yaml
extends: sequence
message: "Don't use '%s'."
tokens:
  - pattern: following
  - tag: NN|NNS|NNP|NNPS
    negate: true
```

### Unexpected behavior when trying to enforce sentence-style headings -- user error, or bug?

1.0 would be the strictest setting and will likely require appropriate use of a [vocab](https://vale.sh/docs/keys/vocab) to avoid false positives related to branding/company-specific terms.

### Help for creating a regex

I feel like you are looking for `nonword: true` setting

https://vale.sh/docs/checks/substitution

The automatic \b added by default are going in the way with your advanced regex

### How to replace captured group with uppercase version

You’ll need to do it [with a script]( https://docs.vale.sh/fixes/suggest#script).

## Vocabularies and spelling

### Export Vocabularies in a Package

You want this:

```ini
StylesPath = vale/styles
MinAlertLevel = suggestion

Packages = https://github.com/spectrocloud/spectro-vale-pkg/releases/latest/download/spectrocloud-docs-internal.zip

**`Vocab` is a global setting**
Vocab = spectrocloud-vocab

[*.md]
**The vocab rules, `Vale.Avoid` and `Vale.Terms`, require the `Vale` style to be enabled.**
BasedOnStyles = Vale, spectrocloud-docs-internal
```

### Which spelling to use?

They are one in the same: `Vale.Spelling` is simply an implementation of `spelling` that uses a built-in dictionary. If you want to use a custom Hunspell-compatible dictionary (maybe for another language or simply one you've made yourself), then you need to create your own `spelling`-based rule to replace `Vale.Spelling`.

There's really no reason to use both, though.

### Best way to ignore spelling for multi-word phrases?

You're correct that, with Vale, spell check is a single-word operation. That said, if I'm understanding correctly, there are two objectives here:

1. You *don't* want "Bananaz" to raise a `spelling` error.
2. You *do* want something like "Blue Bananaz" (essentially, anything that isn't exactly "Yellow Bananaz") to raise a style error.

If this is indeed the case, I would create a custom `spelling`-based rule that excludes the relevant parts of products names:

```yaml
extends: spelling
message: "Did you really mean '%s'?"
level: error
filters:
  - '[Bb]ananaz'
```

And then create another rule, perhaps `Products.yml`, that enforces the correct use of product names:

```yaml
---
extends: substitution
message: "Use '%s' instead of '%s'"
level: error
ignorecase: false
swap:
  # This will catch cases like "Blue Bananaz", "Yellow bananaz", etc.
  '(?:[^\s]*) ?[Bb]ananaz': Yellow Bananaz
```

### Vale.Spelling error message sounds like a suggestion?

You can change this to anything you want by [extending `spelling`](https://vale.sh/docs/topics/styles/#spelling).

### US English spelling seems OK with en_GB spelling?

Both variations of the word are present in Vale's default dictionary. If you want a more strict experience, you can provide a custom dictionary.

### How to structure the archive for a Vale package with scripts and dictionaries

In +v3.0, all non-style resources are store under `<StylesPath>/config`:

```console
$ tree -a MyPackage
MyPackage
├── .vale.ini
└── styles
    ├── MyStyle
    │   └── MyRule.yml
    └── config
        ├── dictionaries
        │   └── MyDic.dic
        ├── scripts
        │   └── MyScript.tengo
        └── vocabularies
            └── MyVocab
                ├── accept.txt
                └── reject.txt
```

I will add an example to the docs.

### Complete Package and Local Repo Vocab

See https://docs.vale.sh/keys/packages#vcs — I think that’s what you’re asking about?

## Configuration and packages

### "Repetition" extension failing?

The problem is the punctuation at the end of "text."; in other words, "text" != "text." You can ignore this kind of difference by adjusting the pattern, for example:

```yaml
extends: repetition
message: "'%s' is repeated!"
level: error
alpha: true
tokens:
  - '[^\s.!?,]+'
```

### "mkdir: No such file or directory" when calling vale sync

found out. it seems we must provide a `styles` folder.

So adding something like: `StylesPath = a/path/to/an/exisiting/folder`
i will report an issue to provide a more explicit error.

### How to extend/override inherited rules

If you want to remove an entry, you can override it by adding it to a [vocabulary](https://vale.sh/docs/topics/vocab/).

If there are words missing from the actual guide you'd like to add, I'd be happy to accept a PR.

### Is anyone using Vale for creative writing (and has suggestions/advice on setup)?

@gacallea I have and do, but… The main issue is the apps you can use Vale in. I tend to keep VSCode for my technical work and a mix of Obsidian, Scrivener, and Ulysses for creative writing.

Vale has an extension for Obsidian, but it doesn't work anymore, and I am trying to fix it. Scrivener used to have an extension ecosystem but doesn't anymore, and Ulysses has something similar to Vale, built with LanguageTool.

I am trying to build a Grammarly Desktop-style tool for Vale that will mean you could access it anywhere, but that is quite some work 😅 

But I guess neovim does have a plugin, so your tooling isn't the question……

### How to disable all rules for one sub-directory?

If you want to ignore a sub-directory entirely, then `--glob` is the way to do.

If you want to change or override config values for a certain sub-directory, you can do so with just the root `.vale.ini` file:

```ini
StylesPath = vale-styles

Packages = Hugo, Microsoft

[*.md]
MinAlertLevel = suggestion
BasedOnStyles = Vale, Microsoft

[content/bad/*.md]
Microsoft.Contractions = NO
Microsoft.We = NO
Vale.Spelling = NO
```

## CI and editors

### How to ignore errors (exit-code) but report them for a portion of files ?

There's no built-in means of doing this. You could write a script that processes Vale's JSON output and changes the exit code according to the file path.

## Other

### Enforcing "One Sentence Per Line"

Something like this should work:

```yaml
extends: occurrence
message: "Only use one sentence per line."
level: error
scope: paragraph
max: 1
token: '[.!?](?: |$)'
```

You could also use [`script`](https://vale.sh/docs/topics/styles/#script) for more fine-grained control.

### How does this tool compares Grammarly's service?

I would say that they are sufficiently different (both in features and use cases) that you’ll likely be unsatisfied if you’re looking for a drop-in replacement.

### Does Vale know parts of speech in the English language?

You can look at the [sequence](https://vale.sh/docs/topics/styles/#sequence) extension point. There are some examples in [Rule Explorer](https://vale.sh/explorer/).

### Capture groups not working in Vale Studio?

You can see the version of Vale being used in the Vale Studio console (https://github.com/errata-ai/vale/issues/707). It hasn't been updated to the latest release yet.

